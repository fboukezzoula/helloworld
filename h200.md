- ⚡ Accelerated Networking
- 🔐 Managed Identity
- 🎮 installation automatique des drivers NVIDIA + CUDA

# Architecture du déploiement

La VM :

- type : Standard_ND96isr_H200_v5
- subnet : rfc-vm-h200
- IP privée uniquement
- Accelerated Networking
- Managed Identity
- extension Azure pour installer automatiquement CUDA + drivers NVIDIA

# terraform complet:

```hcl
terraform {
  required_version = ">=1.5"

  required_providers {
    azurerm = {
      source  = "hashicorp/azurerm"
      version = "~>3.100"
    }
  }
}

provider "azurerm" {
  features {}
}

########################################
# VARIABLES
########################################

variable "resource_group_name" {}
variable "location" { default = "northeurope" }
variable "vm_name" { default = "vm-h200-01" }
variable "admin_username" { default = "azureuser" }
variable "ssh_public_key" {}

variable "vnet_name" {}
variable "subnet_name" { default = "rfc-vm-h200" }

########################################
# EXISTING INFRA
########################################

data "azurerm_resource_group" "rg" {
  name = var.resource_group_name
}

data "azurerm_virtual_network" "vnet" {
  name                = var.vnet_name
  resource_group_name = data.azurerm_resource_group.rg.name
}

data "azurerm_subnet" "subnet" {
  name                 = var.subnet_name
  virtual_network_name = data.azurerm_virtual_network.vnet.name
  resource_group_name  = data.azurerm_resource_group.rg.name
}

########################################
# NETWORK INTERFACE
########################################

resource "azurerm_network_interface" "nic" {

  name                = "${var.vm_name}-nic"
  location            = var.location
  resource_group_name = data.azurerm_resource_group.rg.name

  accelerated_networking_enabled = true

  ip_configuration {
    name                          = "internal"
    subnet_id                     = data.azurerm_subnet.subnet.id
    private_ip_address_allocation = "Dynamic"
  }
}

########################################
# LINUX VM H200
########################################

resource "azurerm_linux_virtual_machine" "vm" {

  name                = var.vm_name
  resource_group_name = data.azurerm_resource_group.rg.name
  location            = var.location
  size                = "Standard_ND96isr_H200_v5"

  admin_username = var.admin_username

  network_interface_ids = [
    azurerm_network_interface.nic.id
  ]

  disable_password_authentication = true

  admin_ssh_key {
    username   = var.admin_username
    public_key = var.ssh_public_key
  }

  #################################
  # Managed Identity
  #################################

  identity {
    type = "SystemAssigned"
  }

  #################################
  # OS Disk
  #################################

  os_disk {
    name                 = "${var.vm_name}-osdisk"
    caching              = "ReadWrite"
    storage_account_type = "Premium_LRS"
  }

  #################################
  # Image Ubuntu
  #################################

  source_image_reference {
    publisher = "Canonical"
    offer     = "0001-com-ubuntu-server-jammy"
    sku       = "22_04-lts"
    version   = "latest"
  }

  boot_diagnostics {}

}

########################################
# NVIDIA CUDA + GPU DRIVER INSTALL
########################################

resource "azurerm_virtual_machine_extension" "nvidia_gpu_driver" {

  name                 = "nvidiaGpuDriverLinux"
  virtual_machine_id   = azurerm_linux_virtual_machine.vm.id
  publisher            = "Microsoft.HpcCompute"
  type                 = "NvidiaGpuDriverLinux"
  type_handler_version = "1.9"

  auto_upgrade_minor_version = true

  settings = <<SETTINGS
{
  "updateOS": true
}
SETTINGS
}
```

# terraform.tfvars

```hcl
resource_group_name = "rg-ai"
vnet_name           = "vnet-ai"

ssh_public_key = "ssh-rsa AAAAB3NzaC1yc2EAAAADAQABAAACAQD..."
```

# Déploiement

```bash
terraform init
terraform plan
terraform apply
```

# Vérification GPU après login (SSH VM)

```bash
nvidia-smi
```


# Points importants avec les H200

⚠️ Pour ces VM GPU très rares :

1️⃣ Quota GPU NDv5 nécessaire

```bash
az vm list-usage -l northeurope -o table
```

2️⃣ Souvent capacity request obligatoire
via support Azure.

3️⃣ Certaines régions nécessitent placement group / proximity pour cluster HPC.



